rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEST MODU: Tüm okuma/yazma işlemlerine izin ver (sadece geliştirme için!)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}